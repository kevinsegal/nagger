/**
 * Public API of the `nagger` npm package. Config files import from here:
 *   import { defineConfig } from "nagger";
 */
export { defineConfig } from "@nagger/core";
export type { NagConfig, NagConfigInput, WatchConfig, NotifyConfig } from "@nagger/core";
export { BRAND } from "@nagger/core";
